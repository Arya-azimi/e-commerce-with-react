type Product = {
  id: number;
  name: string;
  price: number;
  category: string;
  slug: string;
  description: string;
  imageUrl: string;
  isFeatured: boolean;
  createdAt: string;
};

export type { Product };
